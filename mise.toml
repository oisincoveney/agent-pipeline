[tools]
bun = "1.3"

[tasks.test]
run = "bun run vitest run"

[tasks.typecheck]
run = "bun run tsc --noEmit"

[tasks.lint]
run = "bun run check"

[tasks.format]
run = "bun run fix"

[tasks.build]
run = "bun run build:cli"

[tasks."pipeline:run"]
run = "bun src/index.ts pipe"
